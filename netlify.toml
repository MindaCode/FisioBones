[build]
  command = "npm run build"
  # Let the Netlify Next.js plugin determine the correct publish and functions directories.

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
